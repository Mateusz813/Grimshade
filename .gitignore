# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea

# macOS — recursive żeby objąć .DS_Store w subkatalogach
**/.DS_Store

# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
*~
*.bak
.vercel

# Env files — sekrety NIGDY do repo. *.example trzymamy jako template
# (bez wartości). Pattern: każdy `.env.<X>` jest tajny, każdy
# `.env.<X>.example` (i samo `.env.example`) commitujemy jako szablon.
.env
.env.*
!.env.example
!.env.*.example

# TypeScript build cache
*.tsbuildinfo

# Vite PWA dev artifacts (service worker preview)
dev-dist/

# Lint / format cache files
.eslintcache
.prettiercache
.stylelintcache

CLAUDE.md
.claude/

# Test artifacts (vitest + Playwright)
coverage/
test-results/
playwright-report/
playwright/.cache/
